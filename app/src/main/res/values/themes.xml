<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.Material3.Light.NoActionBar">
        <!-- Primary brand color. -->
        <item name="colorPrimary">@color/light_primary</item>
        <item name="colorPrimaryVariant">@color/light_primary_variant</item>
        <item name="colorOnPrimary">@color/white</item>
        <!-- Secondary brand color. -->
        <item name="colorSecondary">@color/light_purple</item>
        <item name="colorSecondaryVariant">@color/teal_700</item>
        <item name="colorOnSecondary">@color/black</item>
        <!-- Customize your theme here. -->
        <!-- Dialog, Cards-->
        <item name="colorSurface">@color/white</item>
        <item name="colorOnSurface">@color/black</item>
        <item name="colorPrimarySurface">@color/light_primary</item>
        <!-- Top tatus Bar-->
        <item name="android:windowLightStatusBar">true</item>
        <item name="android:statusBarColor">@color/white</item>
        <!-- Background window color-->
        <item name="android:windowBackground">@color/white</item>
        <!-- EditText background color-->
        <item name="editTextBackground">@color/light_text_input_background</item>
        <item name="textColorCustom">@color/black</item>
        <item name="mainBgCustom">@color/light_main_variant</item>

        <!--  Gradient Colors-->
        <item name="gradientFirstColor">@color/light_start_color</item>
        <item name="gradientMiddleColor">@color/light_middle_color</item>
        <item name="gradientLastColor">@color/light_last_color</item>

        <item name="navBgColor">@color/white</item>
        <item name="customBackgroundTheme">@color/white</item>
        <item name="customStrokeColor">@color/stroke_color</item>
    </style>

    <style name="AppTheme.FullScreen" parent="AppTheme">
        <item name="android:windowFullscreen">true</item>
    </style>
</resources>